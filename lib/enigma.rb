require 'pry'
require './lib/offset'
require './lib/key'

class Enigma

  def encrypt(message, key = Key.new, date = Offset.new )

  end

  def decrypt(encrypted[:encryption], key, date = Offset.new)

  end

end
